<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ASTC和ETC2</title>
    <url>/xiaomin/2026/01/02/ASTC_ETC2/</url>
    <content><![CDATA[最近项目中遇到了图片发糊的问题，查找发现是跟图片的压缩格式有关。我们是一个老项目，用的是ETC2压缩格式，因为ETC2可以支持市场上绝大多数的设备，特别是一些低端机。我们项目主要是面向东南亚国家，低端机比较多，所以选择了ETC2，但是发展至今，东南亚玩家的设备性能更好了。我们做过一次埋点统计玩家的设备ASTC支持率，几乎占了绝大部分，所以我们选择了压缩质量更好的ASTC。
数据来源：https://developer.android.google.cn/guide/playcore/asset-delivery/texture-compression?hl=zh-cn
让我先分别介绍一下ASTC和ETC2的压缩原理：一、ETC2ETC（Ericsson Texture Compression）最初为移动设备开发，如今它是安卓的标准压缩方案。其原理简单来说，是将4×4 的像素块编码为2×4或4×2像素的上下或者左右两个块的方法，每个块指定一个基色，每个像素的颜色通过一个编码为相对于这些基色偏移的灰度值确定。所以每个块是4 × 4 × 8 &#x3D; 128 bit| A A | B B || A A | B B || A A | B B || A A | B B | 每像素颜色计算：  pixel &#x3D; baseColor + modifier[index]
二、ASTCASTC相比于ETC2有更加灵活的切块策略，可选的有4×4、5×5、6×6、8×8、10×10、12×12。
虽然有这么多的选择，但不变的是每个块依旧是固定128bit。每个块会自适应分区， 分为 1~4 个子区域（subsets），每个区域内颜色渐变平滑。分区方式通过一个 Partition ID 控制（基于预定义的分区模式表，几百种可能）。 每个区域使用两个端点色（endpoint colors），块内像素颜色通过 插值 得到。每个块存储了以下数据：● 分区数量● 分区模式 ID● 端点色模式（bit 深度）● 索引精度 这样 GPU 解码器知道如何按规则重建颜色。解码一个块的流程：

从 16 字节中读取模式信息。
确定分区模式，将像素划分到子区域。
读取并反量化端点色。
根据索引权重在端点色间插值。
得到最终像素颜色。GPU 中这是硬件级并行解码，几乎没有性能损耗。（一）分区数量根据块的尺寸的不同，块的分区数量也不同。一般来说尺寸越大，分区越少。因为每一个分区都需要储存一对端点色和索引精度，而块的大小是固定的，所以分区数量一般与尺寸成反比关系。




块尺寸
常见分区数



4×4
1~3（甚至 4）


6×6
1~2


8×8
1（偶尔 2）


12×12
几乎都是 1


而分区方式有很多种，通过预定义的 Partition ID控制。例如有：横着切 &#x2F; 竖着切 &#x2F; 斜着切 &#x2F; Z 字形 &#x2F; 不规则等。这些灵活的分区方式决定了ASTC具有更精准的颜色插值。（二）端点色模式端点色模式简单来说就是显示端点色颜色的模式，通常有：RGB、L、查分、HDR模式，ASTC可以根据每个像素需要存储的颜色类型来自由选择颜色模式。比如有一个色块只有亮暗变化，那就只需要存储Alpha值，如果这时候还存储RGB颜色就会浪费bit，所以这个时候可以选择亮度模式（L）；而RGB模式可以表达的颜色最自由，所以它适合颜色负责的块。（三）索引精度： 索引精度 &#x3D; 每个像素用多少 bit，来表示它在端点色之间的位置（插值比例）想象你有两种颜色：● 左端：黑色● 右端：白色中间有一个滑块，决定颜色偏向哪一边 低索引精度（2 bit）  ：可选位置：4 个[黑]──┬──┬──┬──[白]      0  1  2  3只能选这 4 个位置之一， 颜色是 跳着变的 高索引精度（6 bit）可选位置：64 个[黑]──────────────────────────────[白]  颜色变化 非常平滑所以索引精度代表这个像素能在俩个端点色中选择用什么颜色，越高，则颜色越平滑。
]]></content>
      <categories>
        <category>Unity开发</category>
      </categories>
  </entry>
  <entry>
    <title>Unity协程的原理</title>
    <url>/xiaomin/2025/10/02/IEnumerator/</url>
    <content><![CDATA[一、协程的概念

协程不是线程  ○ 它不会并行执行，也不会占用系统线程  ○ 全部在 Unity 主线程执行
协程是可暂停的逻辑片段  ○ 使用 IEnumerator，通过 yield return 暂停  ○ 每次暂停时保存当前状态，下次恢复继续执行
协程的作用  ○ 分帧执行逻辑（例如技能前摇、Buff 持续）  ○ 延迟逻辑（等待一段时间或等待物理完成）二、C# 层面的实现机制

Unity 的协程本质依赖 C# 编译器生成的状态机：IEnumerator MyCoroutine(){    print(“开始”);    yield return null;    print(“下一帧继续”);}1️ 编译器转换编译器会把这个方法转换成一个 状态机类：
class MyCoroutineStateMachine : IEnumerator&#123;    int state = 0;    public bool MoveNext()    &#123;        switch (state)        &#123;            case 0:                print(&quot;开始&quot;);                state = 1;                return true; // yield return null            case 1:                print(&quot;下一帧继续&quot;);                state = -1;                return false; // 协程结束        &#125;        return false;    &#125;&#125;
● 每次调用 MoveNext() 就推进协程一次● yield return 本质是：

暂停函数
保存 state
返回一个值给调度器（比如 null, WaitForSeconds, WaitForFixedUpdate）2️ 协程的关键字段● state：记录协程执行到哪一步● Current：存储 yield return 的对象，用于调度器判断何时恢复● MoveNext()：每帧由 Unity 调度调用三、Unity 层面的调度机制Unity 在内部有一个 Coroutine Manager：1️ 调度队列Unity 会维护一个 列表&#x2F;队列，存储所有挂起的协程：List waitingCoroutines● 每个协程对应一个 IEnumerator 状态机● 每帧更新时，Unity 遍历列表：● 判断 Current 返回的对象类型  ○ null → 下一帧恢复  ○ WaitForSeconds → 等待时间到再恢复  ○ WaitForFixedUpdate → 等 FixedUpdate 后恢复  ○ WaitForEndOfFrame → 渲染后恢复2️ 恢复时机yield return 类型	恢复时机null	下一帧 Update 完后 &#x2F; LateUpdate 前WaitForFixedUpdate	下一次 FixedUpdate 结束后WaitForEndOfFrame	本帧渲染完成后WaitForSeconds(x)	等 x 秒后恢复3️ 执行流程以 yield return null 为例：帧 n：


StartCoroutine(MyCoroutine) 调用
IEnumerator 生成状态机，加入等待队列
MyCoroutine 第一次 MoveNext 执行到 yield return null → 暂停，state 保存帧 n+1：
Unity 遍历等待队列
MoveNext() 再次调用 → 协程继续执行
注意：状态机保存了局部变量和执行位置，这就是“跨帧保持上下文”的秘密。四、协程实现的核心原理总结




C# 层面  ○ IEnumerator + 编译器状态机  ○ yield return → 保存状态 + 暂停执行
Unity 层面  ○ Coroutine Manager 维护挂起队列  ○ 每帧遍历判断是否恢复  ○ 恢复时机依赖返回对象类型
特性  ○ 非并行：协程只是在主线程分帧执行  ○ 可跨帧保持局部变量状态  ○ 可延迟逻辑&#x2F;控制执行顺序五、举例：技能前摇系统假设做战斗系统：

IEnumerator SkillCast(){    isCasting &#x3D; true;    &#x2F;&#x2F; 前摇 0.5 秒    yield return new WaitForSeconds(0.5f);
DealDamage(); // 攻击命中
isCasting = false;

}执行原理：

StartCoroutine → 状态机生成 → 入队
MoveNext 执行 → 暂停在 WaitForSeconds
每帧检查时间是否到 → 条件满足 → MoveNext 继续 → DealDamage 执行
状态机结束 → 从队列移除


💡 这样就能实现 分帧执行、延迟效果、状态保持，而不阻塞主线程

六、state 的本质在 Unity &#x2F; C# 协程中：state 保存的是 协程执行到了哪一步。换句话说，它是编译器生成的状态机“指针”，告诉协程“下一次从哪行代码继续执行”。IEnumerator MyCoroutine(){    int x &#x3D; 0;    print(“开始”);    yield return null;     &#x2F;&#x2F; 暂停点 1    x +&#x3D; 1;    print(“下一帧继续”);    yield return null;     &#x2F;&#x2F; 暂停点 2    print(“结束”);} C# 编译器会生成大致这样一个状态机类：  
class MyCoroutineStateMachine : IEnumerator&#123;    int state = 0;      // &lt;--- 保存当前位置    int x;              // &lt;--- 保存局部变量    public object Current &#123; get; private set; &#125;    public bool MoveNext()    &#123;        switch (state)        &#123;            case 0:                x = 0;                 print(&quot;开始&quot;);                Current = null;                state = 1;  // 下一次 MoveNext 从 case 1 开始                return true;            case 1:                x += 1;                print(&quot;下一帧继续&quot;);                Current = null;                state = 2;  // 下一次 MoveNext 从 case 2 开始                return true;            case 2:                print(&quot;结束&quot;);                state = -1; // 协程结束                return false;        &#125;        return false;    &#125;&#125;

总结 state 保存的内容

协程执行位置  ○ 哪个 yield return 暂停了  ○ 下一次 MoveNext() 应该从哪个位置继续
局部变量和参数的快照  ○ 不是直接保存在 state 里，但编译器会把它们变成状态机的成员变量  ○ 保证跨帧保持值
特殊值 -1  ○ 表示协程已经结束为什么需要 state？如果没有 state，协程就没办法跨帧保存上下文：● 你的前摇技能挂起 0.5 秒● 下一帧继续执行 DealDamage● 如果没有 state，就不知道该从哪一行继续，也无法保留局部变量 x七、 体是如何判断继续执行的条件一、协程恢复的核心逻辑
协程本身是一个 IEnumerator 状态机  ○ state 保存位置  ○ Current 保存 yield return 的值
Unity 每帧会遍历挂起的协程队列，查看 每个协程的 Current 返回值，决定是否可以调用 MoveNext() 继续执行。所以判断“是否继续执行”，完全依赖 yield 返回的对象类型。二、不同 yield 对应的条件




yield return 类型
Unity 判断条件（何时调用 MoveNext）
说明



null
下一帧 Update 结束
跨一帧，立即恢复


WaitForSeconds(seconds)
等待累计时间 &gt;&#x3D; seconds
Unity 内部计时器，每帧累加 deltaTime，时间到恢复


WaitForFixedUpdate
FixedUpdate 完成
下一次物理帧结束后恢复


WaitForEndOfFrame
本帧渲染完成
在渲染管线末尾恢复（LateUpdate 后）


自定义 yield 对象（ICustomYieldInstruction）
keepWaiting &#x3D;&#x3D; false
自定义条件判断，返回 false 时恢复


三、内部原理假设协程队列是这样：List waitingCoroutines; 每帧大致伪代码：  
for each coroutine in waitingCoroutines:    object yieldObj = coroutine.Current;        if (CanResume(yieldObj)):        bool hasNext = coroutine.MoveNext();        if (!hasNext) remove from waitingCoroutines

CanResume(yieldObj) 的逻辑：  
bool CanResume(object yieldObj)&#123;    if (yieldObj == null) return true; // 下一帧立即恢复    if (yieldObj is WaitForSeconds wfs) return wfs.timeElapsed &gt;= wfs.duration;    if (yieldObj is WaitForFixedUpdate) return FixedUpdateDoneThisFrame;    if (yieldObj is WaitForEndOfFrame) return EndOfFrameReached;    if (yieldObj is ICustomYieldInstruction c) return !c.keepWaiting;    return false;&#125;]]></content>
      <categories>
        <category>Unity开发</category>
      </categories>
  </entry>
</search>
